[2025-07-07 22:26:51] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-07-01' AND '2025-07-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:27:20] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-08-01' AND '2025-07-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:27:29] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-11-01' AND '2025-12-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:28:28] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-08-01' AND '2025-07-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:28:29] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-08-01' AND '2025-07-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:28:34] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-08-01' AND '2025-12-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:28:55] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-08-01' AND '2025-07-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
[2025-07-07 22:29:00] SELECT 
                    m.annee,
                    m.mois,
                    CONCAT(m.annee, '-', LPAD(m.mois, 2, '0')) AS periode,
                    COUNT(DISTINCT m.id_pret) AS nombre_prets,
                    SUM(m.capital) AS capital_total,
                    ROUND(SUM(m.interet), 2) AS interets_mensuels
                FROM mensualite m
                JOIN pret p ON m.id_pret = p.id_pret
                WHERE DATE(CONCAT(m.annee, '-', LPAD(m.mois, 2, '0'), '-01')) BETWEEN '2024-08-01' AND '2025-12-31'
                GROUP BY m.annee, m.mois
                ORDER BY m.annee, m.mois
